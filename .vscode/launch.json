{
  // 使用 IntelliSense 了解相关属性。
  // 悬停以查看现有属性的描述。
  // 欲了解更多信息，请访问: https://go.microsoft.com/fwlink/?linkid=830387
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch via NPM",
      "request": "launch",
      "runtimeExecutable": "pnpm",
      "args": ["run", "start:dev"],
      "skipFiles": ["<node_internals>/**"],
      "type": "node",
      "console": "integratedTerminal",
      // 根目录
      // "cwd": "${workspaceFolder}/access_token_and_refresh_token"
      // "cwd": "${workspaceFolder}/article-views"
      // "cwd": "${workspaceFolder}/schedule-task"
      "cwd": "${workspaceFolder}/debug-nest-source"
    }
  ]
}
